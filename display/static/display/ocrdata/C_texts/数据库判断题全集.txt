能唯一决定一个元祖且无多余属性的属性集称为候选键。√数据库技术的奠基人之一E.f.Codd从1970年起发表过多篇论文，主要论述的是关系数据模型。√在关系数据模型中，实体与实体之间的联系统一用二维表表示。√同一个关系模型的任意二个元组值可以完全相同。×一个关系中的主键的取值可以为空值（Null）×外键一定是同名属性，且不同表中的同名属性也一定是外键。×关系模式中属性的数目称为关系的元数，又称为关系的目。√在关系理论中，把能够唯一地确定一个元组的属性或属性组合称为域。×关系中同一列的数据类型可以相同，也可以不同。×关系运算的运算对象一定是关系，但运算结果不一定是关系。×设有关系R1和关系R2，经过关系运算得到结果S，则S是一个表单。×在并运算中，重复的元组取且仅取一次。√对一个关系做选择操作后，新关系的元组个数小于或等于原来关系的元组个数。√投影操作时对表进行水平方向的分割。×在关系数据库的基本操作中，把两个关系中相同属性值的元组连接到一切，形成新的二维表的操作成为联接。而且，等值联接和自然联接是一回事。×在关系查询中，利用关系的投影，选择和联接运算可以方便地分解或构造新的关系。√关系数据库管理系统应能实现的专门关系运算包括：排序，索引和统计。×建立数据库中的表时，将年龄字段值限制在18-25岁之间。这种约束属于参照完整性约束。×关系模型中有三类完整性约束，并且关系模型必须满足着三类完整性约束条件。×对数据库的维护包括保持数据的完整性，一致性和安全性。√修改表结构在设计视图中完成，编辑表记录只能在数据表图中完成。√格式属性用来决定数据的显示方式和打印方式，既改变数据输出的形式，也改变数据的存储格式。×数据表既可以再不同数据库间复制，也可以再同一个数据库下复制。√在数据表视图下，每个字段的显示宽度受“字段大小”属性的影响，用户不能随意更改字段的显示宽度，以免造成数据丢失。×在Access中，不仅可以按一个字段排序记录，也可以按多个字段排序记录。√记录筛选是把表中符合条件的记录显示出来，不符合条件的记录删除。×按窗体筛选时，同一行的条件之间是“与”的关系，设置在不同行的条件之间是“或”的关系。√两表间的关系删除的同时，这两个表也被自动删除。×一对一的关系可以合并，多对多的关系可拆成两个一对多的关系。因此，表间的关系可以都定义为一对多的关系。√在定义表间关系时，应设立一些准则。这些准则将有助于维护数据的完整性。参照完整性就是在输入，删除或更新记录时，为维持表之间已经定义的关系而必须遵循的规则。√当我们改变“表”中的数据时，“查询”中的数据也会发生相应的改变。√我们可以根据表来建立查询，但不可以根据某一个查询来建立新的查询。×Access2003的每一个查询主要有三个视图，即“数据表”视图，“设计”视图和“SQL”视图。√“SQL”视图用来显示与“设计”视图等效的SQL语句。√查询的“数据表”视图看起来很像表，它们之间是没有什么差别的。×在查询的数据表中科院改变列宽和行高，还可以隐藏和冻结列。√使用选择查询可以从一个或多个表或查询中检索数据，可以对记录组或全部记录进行求总计，计数等汇总运算。√建立多表查询的两个表必须有相同的字段，通过这个相同的字段建立起两个表之间的关系。√Access2003中的查询准则主要有函数和表达式2中。×Access2003提供了“表达式生成器”，用户可以再需要帮助的时候，在设计网格中的“条件”行单元格中启动“表达式生成器”。√只有利用“设计”视图建立的查询，建立后才可以查询进行编辑修改。×可以对查询的结果进行排序。√使用查询“设计”视图中的“条件”行，可以对查询中的全部记录或记录组计算一个或多个字段的统计值。使用“总计”行，可以添加影响结果的条件表达式。×计算字段是指将已有的字段通过使用表达式而建立起来的新字段。√Access支持一种特殊类型的总计查询，叫做交叉表查询。利用该查询，你可以在类似电子表格的格式中查看计算值。√建立交叉表查询的方法有两种：使用交叉表查询向导和使用“设计”视图。√使用“交叉表查询向导”建立交叉表查询时，使用的字段可以属于不同的表或查询。×执行参数查询时，数据库系统显示所需参数的对话框，由用户输入相应的参数值。√查询在运行过程中对原始表不能做任何修改。×操作查询是指在一个操作中只能更改一条记录的查询。×SQL命令能在Access中使用，也能在其他高级语言中使用。√使用SQL语言的CREATETABLE命令可以直接建立表。√在SQL中，ALTER命令有两个选项，其中MODIFY用于修改字段的类型和宽度等，ADD用于添加字段。×SQL只能查询数据，不能修改数据。×当进行多表查询时，既可以在JOIN中建立连接条件，也可以在WHERE中建立连接条件。√在进行数据查询时，不能指定查询结果的标题，只能用字段名作标题。×在进行数据查询时，可以指定查询结果的顺序，且只能降序排列不能升序排列。×可以将查询结果送入一个新表中。√当用SELECT进行查询时，结果可以是字段值，也可以使统计值。√可以用UNION将两个查询记过合并为一个查询结果。√在一个窗体中能显示多条记录的窗体只有表格式这种类型的窗体。×数据透视表窗体对数据进行的处理是Access其他工具无法完成的。√当源数据发生更改时，数据透视表中的数据不会随之更新。×在Access中，属性用于决定表，查询，字段，窗体及报表的特性。√标签不显示字段或表达式的数值，它没有数据来源。√文本框主要用来输入或编辑字段数据，是一种交互式控件。其中，结合型文本框一般用来显示提示信息或接受用户输入数据等。×如果要选中多个控件，可以按Ctrl键，再依次单击各个控件。×窗体的主题节位于窗体的中心部分，是工作窗口的核心部分，由多种窗体控件组成。√窗体事件是指操作窗体时引发的事件，如：打开，关闭，加载和取消事件。×窗体的“数据输入”属性值需在“是”和“否”两个选项中选取。如果选取“是”，则在窗体打开时，只显示一个空记录，否则显示已有的记录。√报表主要是用来输入数据的。×报表与它基于的表或者查询不相互作用。√报表可以执行简单的数据浏览和打印功能，但不能对原始数据进行比较，汇总和小计。×在报表中可以嵌入图片。√一个报表中可以仅包含一个“报表页眉”节。×报表能够改变数据库的基本数据。×报表和窗体没有任何区别，报表可以完成窗体的所有工作。√报表不仅可以包含子报表还可以包含子窗体。√在Access中，创建报表的方法基本上有三种，分别是:自动创建报表，使用向导和使用设计视图。√报表快照是包含了Access报表每页的高保真副本。报表快照可以使报表及时地在网上发布。√